import { ShortNumberSuffixPipe } from './short-number-suffix.pipe';

describe('ShortNumberSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNumberSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
